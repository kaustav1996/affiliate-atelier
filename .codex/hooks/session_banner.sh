#!/bin/bash
echo "--- ScentForge Atelier ---"
echo "Local skills available:"
echo "  1. scentforge-add-product: add a perfume with image generation, DB upsert, seed data, UI verification, and tests."
echo "  2. scentforge-run-local: start Docker Postgres, reset/migrate/seed clean data, and run the Next.js app locally."
echo "Design skill installed: pbakaus/impeccable under .agents/skills/impeccable."
echo "Rule: before asking the user to test, run npm run demo:reset so dashboard sales/commission are clean."
echo "Rule: use Impeccable design context from .impeccable.md for visual work."
